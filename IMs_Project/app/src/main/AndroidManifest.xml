<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adimas_8.ims_project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/itera_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/itera_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Form_Register" />
        <activity android:name=".Form_Login" />
        <activity android:name=".Main_Menu" />
        <activity android:name=".Detail" />
        <activity android:name=".Form_Peminjam"/>

    </application>

</manifest>